check if uv is intalled in global py.
create a uv virtual env


-------------------------------------------------   creates pyproject.toml
uv init LiveKit_py --bare (creates the folder)
cd LiveKit_py

uv init --bare . (init in existing folder, folder must be empty)



=================================================
--active switch installs in active venv
=================================================



-------------------------------------------------   STT-LLM-TTS pipeline, noise cancellation, and turn detection: adds .venv folder with these & uv.lock
uv add \
  "livekit-agents[silero,turn-detector]~=1.2" \
  "livekit-plugins-noise-cancellation~=0.2" \
  "python-dotenv"


uv add --active "livekit-agents[silero,turn-detector]~=1.2" \
           "livekit-plugins-noise-cancellation~=0.2" \
           "python-dotenv"

ADDS TO YOUR VENV

------------------------------------------------- adds .env.local
lk app env -w
connect to livekit via lk, your auth and download info

-------------------------------------------------
uv run agent.py download-files
uv run --active agent.py download-files


Running in console
  uv run agent.py
  uv run --active agent.py


Running in dev
  uv run agent.py dev
  

-------------------------------------------------


uv add "livekit-agents[speechmatics]~=1.2"
uv add --active "livekit-agents[speechmatics]~=1.2"

uv add "livekit-agents[elevenlabs]~=1.2"
uv add --active "livekit-agents[elevenlabs]~=1.2"

uv add "livekit-agents[openai]~=1.2"
uv add --active "livekit-agents[openai]~=1.2"